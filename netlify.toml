[build]
  command = "npm run build"
  publish = "dist"

# Optional: redirect 404s to index.html for SPA-like routing (not strictly needed here)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
